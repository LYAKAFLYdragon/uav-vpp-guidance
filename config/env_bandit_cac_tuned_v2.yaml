includes:
  - env_bandit.yaml

env:
  bandit:
    controller_type: close_air_combat
    close_air_combat:
      guidance_config:
        # More aggressive "position-and-separate" tuning.
        desired_range_lag: 5000.0
        desired_range_lead: 12000.0
        lag_distance: 3000.0
