# Pilot: End-to-End method under maneuvering target.
includes:
  - maneuver_target_pilot_base.yaml

experiment:
  name: maneuver_target_e2e_pilot_s0

end_to_end:
  enabled: true

virtual_point:
  enabled: false
  anchor_mode: current_target
  return_info: true
  action_dim: 3
  d_long_range: [-1500.0, 1500.0]
  d_lat_range: [-800.0, 800.0]
  d_vert_range: [-500.0, 500.0]
  smoothing_alpha: 0.3
