# Stage 9A Remote Audit / Fix

## 时间: 2026-06-07 11:05

## Audit 项与结果

| 检查项 | 结果 |
|---|---|
| `git fetch origin && git log --oneline --decorate -20` | `origin/main` 与 `HEAD -> main` 均为 `32929a8` (Stage 9A)。远程 `refs/heads/main` 一致。 |
| `src/uav_vpp_guidance/envs/tracking_env.py` current_gains | ✅ 从 `config.get("guidance", {}).get("gains", {})` 初始化，非硬编码默认值。 |
| `scripts/run_paper_benchmark.py` Stage 9A 交付 | ✅ 含 `--checkpoint-map`、`_resolve_checkpoint`、`_load_gain_only_gains` schema 校验、`generate_manifest`、`run_manifest.json` 输出。 |
| `tests/test_paper_benchmark.py` Stage 9A 交付 | ✅ 含 `TestCheckpointResolution`、`TestGainSchemaValidation`、`TestUnknownMethod` subprocess、`TestRunManifestArtifact`、`TestGainsAffectTelemetry`。 |
| `README.md` Stage 9A 交付 | ✅ Stage 8C complete、Stage 9A current；Experiment Phase Order / Benchmark Types 表格；Section 7 roadmap 状态更新。 |
| `memory/2026-06-07-1045.md` | ✅ 存在且记录 Stage 9A 关键决策。 |

## 环境修复
- **问题**: `outputs/experiments/no_prediction_vpp_ppo/checkpoints/best.pt` 缺失，导致无法运行 paper-safe benchmark（`paper_safe=false`）。
- **修复**: 将存在的 `outputs/audit_no_pred_final/checkpoints/best.pt` 复制到 config 期望的路径，使 4 个 method 的 checkpoint 全部可用。
- **影响**: 无代码变更；`outputs/` 在 `.gitignore` 中，不影响仓库状态。

## Paper-Safe Benchmark 验证
```bash
python scripts/run_paper_benchmark.py \
  --backend simple --seeds 0 1 --scenarios regression \
  --output-dir outputs/paper_benchmark_papersafe_9a
```

产出：
- `outputs/paper_benchmark_papersafe_9a/summary.md`
- `outputs/paper_benchmark_papersafe_9a/results.csv`
- `outputs/paper_benchmark_papersafe_9a/tables/comparison_table.md`
- `outputs/paper_benchmark_papersafe_9a/figures/figure1_method_comparison.png`
- `outputs/paper_benchmark_papersafe_9a/run_manifest.json`

`run_manifest.json` 关键字段：
- `git_commit`: `32929a8`
- `paper_safe`: `true`
- `invalid_for_paper_reasons`: `[]`

所有方法的 `checkpoint_exists=true`，`gain_only` 的 `gains_schema_valid=true`，`loaded_gains` 非空。

## 测试汇总
- `python -m compileall src scripts tests` → pass
- `pytest tests/test_paper_benchmark.py -q` → 34 passed
- `pytest tests/ -x --tb=short` → 869 passed, 0 failed, 23 warnings

## 结论
Stage 9A 远程状态一致，基线已锁死，满足进入 Stage 9B 的前置条件。
